################################################################################
## BASH aliases
################################################################################

# ls aliases:
alias ll='ls -l'
alias la='ls -A'
alias l='ls -CF'

# emacs
alias emacs="${HOME}/.local/bin/emacs"
